[build]
  publish = "dist"
  command = "pnpm build"

# React Router SPA: serve index.html for client routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "24"
