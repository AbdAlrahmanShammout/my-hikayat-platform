export enum PlanKind {
  FREE = 'free',
  MONTHLY_PAID = 'monthly_paid',
}

export enum PlanInterval {
  MONTH = 'month',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  CANCELED = 'canceled',
}
