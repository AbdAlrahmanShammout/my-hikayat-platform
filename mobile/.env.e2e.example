# Mobile UI E2E (Maestro + Android Emulator).
# Copy to mobile/.env.e2e — never commit .env.e2e.
#
# Android Emulator reaches the host machine via 10.0.2.2 (not localhost).
# Match APP_PORT from backend/.env.e2e (default 3001).

EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:3001
